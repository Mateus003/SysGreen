<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context=".ResetPasswordActivity">
    <ImageView
        android:id="@+id/image_reset_password"
        android:layout_width="200dp"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_reset_password"/>
    <TextView
        android:id="@+id/text_password"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:textSize="17sp"
        android:textColor="@color/black"
        android:fontFamily="@font/secular_one"
        android:layout_below="@id/image_reset_password"
        android:layout_centerHorizontal="true"
        android:text="Esqueceu sua senha?"/>
    <TextView
        android:id="@+id/text_reset_password"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:textColor="@color/black"
        android:layout_below="@id/text_password"
        android:fontFamily="@font/nunito"
        android:text="Enviaremos um link para o e-mail cadastrado no nosso app para que você possa redefinir sua senha:)"
    />

    <TextView
        android:id="@+id/text_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/text_reset_password"
        android:layout_marginTop="30dp"
        android:fontFamily="@font/nunito"
        android:text="E-mail"
        android:textColor="@color/black"
        android:textSize="17sp" />

    <EditText
        android:id="@+id/edittext_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/text_email"
        android:drawableEnd="@drawable/ic_email" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/button_sent_email"
        android:layout_width="200dp"
        android:layout_marginTop="20dp"
        android:layout_height="wrap_content"
        android:background="@drawable/background_button_login"
        android:text="Redefinir senha"
        android:layout_centerHorizontal="true"
        android:textColor="@color/white"
        android:layout_below="@id/edittext_email"/>
</RelativeLayout>
